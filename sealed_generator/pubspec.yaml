name: sealed_generator
description: Dev-dependency to generate sealed classes and when statements together with the sealed.dart package.
version: 0.0.1
homepage: https://github.com/xsahil03x/sealed.dart

authors:
  - Sahil Kumar <xdsahil@gmail.com>
  - Jai Sachdeva <sachdeva.jai@outlook.com>

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:

  # Specify a specific version or a version constraint
  # for "sealed" before publishing the package.
  sealed:
    path: ../sealed/
  source_gen: ^0.9.4+6
  build: ^1.2.1
  analyzer: ^0.39.1
  code_builder: ^3.2.0
  dart_style: ^1.3.3


dev_dependencies:
  test: ^1.9.4
  build_runner: ^1.7.1
  source_gen_test: ^0.1.0+6